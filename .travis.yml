language: java
sudo: required

env:
  global:
    - BUILD_NUMBER: "$TRAVIS_BUILD_NUMBER"

cache:
  directories:
    - "$HOME/.gradle"
    - ".gradle/wrapper"
    - ".gradle/caches"

jobs:
  fast_finish: true
  jdk: openjdk11
  before_script:
    - chmod +x gradlew
  script:
    - ./gradlew build

